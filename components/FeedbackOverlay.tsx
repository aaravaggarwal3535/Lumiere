import React from 'react';

// This component is currently superseded by the inline Chat UI in CameraView.
// Kept for potential future "AR Mode" implementations.

export const FeedbackOverlay: React.FC = () => {
  return null;
};